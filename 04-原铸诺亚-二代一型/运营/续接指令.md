# 原铸诺亚 · 续接指令

> 读此文件 = 明白当前状态。3 步可开工。

---

## 第一步：读版本

```
cat ~/noah-档案馆/原铸诺亚/运营/VERSION.json
```

全局版本 v2.0.0，7 个 NCP 模块全部 active。版本已写入 PG `version_registry` 表，仪表盘可读。

## 第二步：读项目注册表

```bash
cat ~/noah-档案馆/原铸诺亚/项目注册.md
```

7 个 NCP 模块明细 + 文件结构 + 续接方法。看 `模块/` 和 `运营/` 目录结构确认完整性。

## 第三步：读活跃 TFC

当前中断在 **完成「原铸诺亚完整管理体系」全量交付**。用该文件提供的信息继续推进。

### 活跃 TFC 任务

原铸诺亚 project 下待处理的任务：
- **TFC-078** Phase 2a — 工具管理模块 **(active)**
- **TFC-079** Phase 2b — TEL/MAIL 协议引擎 **(pending)**
- **TFC-080** Phase 3a — 认知记忆底座 **(pending)**
- **TFC-081** Phase 3b — 可插拔路由策略 **(pending)**
- **TFC-082** Phase 4 — 可观测性+告警+插件系统 **(pending)**
- **TFC-085** 完全体诺亚保险计划 **(pending)**

> 验证指令: `psql -d noah_local -c "SELECT id, title, status FROM tfc_tasks WHERE project='原铸诺亚' AND status='active' ORDER BY id;"`

### 核心资源路径

| 资源 | 路径 |
|------|------|
| 项目注册 | `~/noah-档案馆/原铸诺亚/项目注册.md` |
| 产品手册 | `~/noah-档案馆/原铸诺亚/产品手册.md` |
| 功能矩阵 | `~/noah-档案馆/原铸诺亚/运营/功能矩阵.md` |
| CHANGELOG | `~/noah-档案馆/原铸诺亚/运营/CHANGELOG.md` |
| 模块定义 | `~/noah-档案馆/原铸诺亚/模块/NCP-*.md` |
| 仪表盘 | `http://localhost:8899/原铸诺亚-仪表盘.html` |
| TFC 总览 | `psql -d noah_local -c "SELECT * FROM tfc_tasks ORDER BY id DESC LIMIT 5;"` |
| 版本注册表 | `psql -d noah_local -c "SELECT * FROM version_registry ORDER BY id;"` |

### 常用验证命令

```bash
# 系统健康
hermes --version
psql -d noah_local -c "SELECT COUNT(*), MIN(verified_at) FROM version_registry;"
curl -s http://localhost:8899/原铸诺亚-仪表盘.html | head -3

# 模块文件完整性
ls ~/noah-档案馆/原铸诺亚/模块/
ls ~/noah-档案馆/原铸诺亚/运营/

# TFC 活跃任务
psql -d noah_local -c "SELECT id, title, status FROM tfc_tasks WHERE project='原铸诺亚' ORDER BY id;"
```

## 行为规则（存续）

1. **所有状态声称必须先验证再输出** — 执行 `do-verify-report` 协议
2. **版本号修改必须同步更新三处**：`VERSION.json` + PG `version_registry` + 对应 NCP 模块文件
3. **推进前先读此文件** — 避免重复造轮子或走错方向
